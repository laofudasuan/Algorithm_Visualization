---
title: 自动机
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';
import GraphAlgorithmVisualization from '../../../../components/visualizations/GraphAlgorithmVisualization';

## 什么是自动机？

自动机是一种“小计算机”模型，它把一件事物的发展设计成一个简单的规则地图

<BoxWithTag tag="自动机的概念">
自动机可以形式化地定义为：
- 状态（States）：系统可能处于的各种情况
- 输入字母表（Alphabet）：允许输入的符号集合
- 转移函数（Transition Function）：定义如何根据当前状态和输入转移到下一个状态
- 初始状态（Start State）：系统开始时的状态
- 接受状态（Accept States）：表示成功/终止的状态集合

简单理解，状态就是一个地图的若干点，转移函数就是每个点转移的边，初始状态是起点，接受状态是终点
</BoxWithTag>

### 案例：自动售货机

例如，我们生活中常常见到的自动售货机，可以用如下自动机来描述：

<div className="flex-1">
    <GraphAlgorithmVisualization 
      graphName="AM2" 
      animationList={[]}
      enableDrawing={false}
    />
</div>

### 案例：抛硬币检测机

举例：我们不停地抛一枚硬币，如果连续三次抛出的结果相同就停止

把每次抛的结果作为自动机的输入，可以构造如下自动机：

<div className="flex-1">
    <GraphAlgorithmVisualization 
      graphName="AM" 
      animationList={[]}
      enableDrawing={false}
    />
</div>

### 状态机思维

状态机思维是一种代码实现的方式，我们编程时不再构思分支/循环/递归等东西，而是聚焦于：

- 我当前是什么状态？
- 发生了什么？

根据以上两者来决定

- 下一步我该做什么？
- 做完后我的新状态是什么？

简单来说就是：

根据当前状态和输入，决定做什么并切换到下一个状态

##### abc446_e E - Multiple-Free Sequences 

在整数对 $(x, y)$ 满足 $0 \leq x, y \leq M-1$ 的范围内，有多少对使得由以下递推关系定义的无限序列 $(s_1, s_2, \ldots)$ 中不包含 $M$ 的倍数？

- $s_1 = x$
- $s_2 = y$
- $s_n = A s_{n-1} + B s_{n-2}$ $(n \geq 3)$

- $2 \leq M \leq 1000$
- $0 \leq A, B \leq M-1$
- 所有输入值均为整数。

<CollapsibleComponent title="做法"> 

把$(x,y)$当作自动机的状态，建一个自动机。答案就是有哪些点所在的环不经过$x=0||y=0$的点
</CollapsibleComponent>

## 参考资料

<a href="https://oi-wiki.org/misc/fsm/" target="_blank" rel="noopener noreferrer" className="text-blue-600 hover:underline inline-flex items-center gap-1">🔗 OI-Wiki关于自动机的定义</a>
