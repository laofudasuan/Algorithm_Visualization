---
title: KMP自动机
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';

# 模式匹配问题

给定字符串$S,T$，判断$T$是否作为$S$的子串出现过

自动机方法：对$T$构造一个自动机，使得当$S$在自动机中运行时，如果出现了$T$就会走到结束态，如果没有出现$T$就不会走到结束态

##### P3193 HNOI2008 GT考试

阿申准备报名参加 GT 考试，准考证号为 $N$ 位数$X_1,X_2…X_n\ (0\le X_i\le 9)$，他不希望准考证号上出现不吉利的数字。
他的不吉利数字$A_1,A_2,\cdots, A_m\ (0\le A_i\le 9)$ 有 $M$ 位，不出现是指 $X_1,X_2\cdots X_n$ 中没有一段恰好等于 $A_1,A_2,\cdots ,A_m$，$A_1$ 和 $X_1$ 可以为 $0$。

$N\leq10^9$，$M\leq 20$，$K\leq10000$。

###### 输入格式

第一行输入 $N,M,K$ 接下来一行输入 $M$ 位的数。

###### 输出格式

阿申想知道不出现不吉利数字的号码有多少种，输出模 $K$ 取余的结果。

###### 输入输出样例 #1

### 输入 #1

```
4 3 100
111
```

### 输出 #1

```
81
```


<CollapsibleComponent title="做法"> 
设状态$f[i][j]$表示考虑了前$i$位，当前匹配的长度为$j$的方案数

转移：枚举第$i+1$位是什么，然后用KMP求出新的匹配长度

注意到这个DP中$j$这一维很小，所以可以把这一维写成矩阵的形式
</CollapsibleComponent>

##### CF1163D Mysterious Code

##### CF1015F Bracket Substring

##### CF808G Anthem of Berland
