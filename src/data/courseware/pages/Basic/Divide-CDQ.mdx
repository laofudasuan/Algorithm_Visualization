---
title: CDQ分治
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';

### CDQ分治

##### P3810 三维偏序（陌上花开）
    考虑如何用分治方法解决三维偏序

    先考虑二维偏序也就是$a_j \le a_i,b_j \le b_i$怎么做

    先按$a$为第一关键字，$b$为第二关键字排序，那么我们就保证了第一维$a$的有序。

    于是，对于每一个$i$，只可能$1$到$i-1$的元素会对它有贡献，那么直接查$1$到$i-1$的元素中满足$b_j \le b_i$的元素个数。

    具体实现？动态维护b的树状数组，从前到后扫一遍好啦，$O(n\log n)$。

    那么三维偏序呢？我们只有在保证前两维都满足的情况下才能计算答案了。
    


##### “CDQ分治”

    “CDQ分治”的主要过程就是如果有一维，只有前面的对后面的有贡献，我们可以分治这一维
    
    每次分治时计算左边对右边的贡献，然后递归下去
    
    这样一直递归到叶子，会发现所有贡献计算完毕

    仍然按$a$为第一关键字，$b$为第二关键字，$c$为第三关键字排序，第一维保证左边小于等于右边了。
    
    为了保证第二维也是左边小于等于右边，我们还需要排序。
    
    考虑类似归并排序的过程中，统计出在子问题中产生的对答案贡献



##### “CDQ分治”

    现在我们有一个序列，我们把它递归分成两个子问题，子问题进行完归并排序，已经保证$b$有序。此时，两个子问题间有一个分界线，原来第一维左边小于等于右边，所以现在分界线左边的任意一个的$a$当然还是都小于右边的任意一个。只有分界线左边的能对右边的产生贡献。

    于是，问题降到了二维。
    
    发现是以$\log$的代价使得维数减少了一维，每层分治时需要计算左边对右边的贡献。

    
    这样每层变成一个二维问题。
    
    我们可以归并排序（左边的指针为$j$，右边的为$i$）并维护$c$的树状数组，如果当前$b_j \le b_i$，说明j可以对后面加入的满足$c_j \le c_i$的$i$产生贡献了，把$c_j$加入树状数组；否则，因为后面加入的$j$都不会对i产生贡献了，所以就要统计之前被给的所有贡献了，查询树状数组$c_i$的前缀和。
    
    总时间复杂度$O( n\log^2n )$



##### “CDQ分治”
    总结：
    
    “CDQ分治”就是指在一维上只有前面对后面有贡献，比如数据结构只有时间前面的修改对时间后面的询问有贡献，也可以使用这个算法，然后分治这一维，分治的每层需要维护分治中线左边对右边的贡献。
    
    分治的复杂度：$T(n)=2T(n/2)+O(n)=O(n\log n)$



##### P4027 NOI2007 Cash
    
    有两种金券，金券按比例交易：
    
    买入时，将投入的资金，购买比例为$Rate[i]$的两种金券
    
    卖出时，卖出持有的一定比例的金券。已知未来$n$天两种的金券价格$A[i]$、$B[i]$，初始资金为$s$，求最大获利。

    为使获利最大，交易时显然应该全部买进或卖出。
    
    $1 \le n \le 10^5$


##### P4027 NOI2007 Cash
    简要做法：

    令$F[i]$表示第$i$天获得的最大$B$卷数量。
    枚举上一次交易日$j$
    $$F[i]=\frac{\max(ans,Rate[j]\times F[j]\times A[i]+F[j]\times B[i])}{Rate[i]\times a[i]+b[i]}$$
    
    $O(n^2)$
    
    观察括号内的表达式，可以发现决策$j$优于决策$k$的条件：
    
    $$\frac{Rate[j]\times f[j]-Rate[k]\times f[k]}{f[j]-f[k]} >-\frac{b[i]}{a[i]}$$

    上面这个式子的左边，一般记成$slope(j,k)$


##### P4027 NOI2007 Cash cdq分治
    注意到，这个式子是可以斜率优化的

    $$Slope(j,k) > -\frac{b[i]}{a[i]}$$
    
    令$G[i] = Rate[i] \times f[i]$，在二维平面上定义点$X[i]=(F[i],G[i])$
    
    $Slope(j,k)$就是通过$X[j]$和$X[k]$的斜率


##### P4027 NOI2007 Cash cdq分治

    维护一个点集X，支持以下两个操作：
    
    
        - 在第一象限的任意位置插入一个点
        - 给定负数斜率$K$，求所有斜率为$K$且过点集中任意点的直线在$Y$轴上的最大截距
    
    
    操作2最终用到的点都会在点集的上凸壳上
    
    维护点集X的凸包，支持动态插入和斜率查询

    但是，插入的点坐标不单调

    怎么办？cdq分治，分治完以后按照坐标排序


##### P4027 NOI2007 Cash cdq分治
    定义过程Solve$(L,R)$

    假设运行Solve$(l,r)$可以得到$F[l]$到$F[r]$的值。

    $[L,mid]$区间里的询问，可以直接递归Solve$(l,mid)$解决。

    $[mid+1,r]$区间里的询问$K$，会受到$[mid+1,k]$这些点的影响，以及$[l,mid]$的影响。前半部分可以递归解决。

    Solve$(l,r)$
    
    递归调用Solve$(l,mid)$

    整体考虑$[l,mid]$间的点对$[mid+1,r]$间询问的影响。
    
    递归调用Solve$(mid+1,r)$



##### P4027 NOI2007 Cash cdq分治
    整体考虑$[l,mid]$对$[mid+1,r]$的影响

    给定点集$X$和一系列询问

    每个询问是一个负数斜率
    
    回答所有斜率符合且通过点集$X$中任意点的直线中，$Y$轴的最大截距是多少

    只要考虑点集$X$的上凸包。
    
    对于每个询问，在凸包上二分即可。

    这一步的复杂度是$O(n\log n)$，这里$n=r-l$。



##### P4027 NOI2007 Cash cdq分治
    
    时间复杂度？
    
    Solve$(l,r)$的复杂度是$O(n\log n)$
    
    $T(n)=2T(n/2)+O(n\log n)$
    
    $T(n)=O(n\log^2n)$

    
    离最优化还有距离。

    需要$\log n$的地方
    
    
        - 求点集凸包
        - 二分答案
    


##### P4027 NOI2007 Cash cdq分治
    
    
        - 点集凸壳
            
        合并两个凸壳的时间复杂度是$O(n+m)$
    
        Solve$(l,r)$结束后返回$[X[l] \ldots X[r]]$的凸壳

        单步$O(n)$，总体$O(n \log n)$
        - 二分答案
        
        放弃二分，离线处理
        
        把点集凸壳和所有询问排序，用两个指针扫描
        - 对询问排序
        
        提前对询问进行一次归并排序，存储所有中间结果
        
        为什么不能在Solve()时进行归并？
        
        预处理复杂度$O(n\log n)$,主递归中单步$O(n)$

        $T(n)=2T(n/2)+O(n),T(n)=O(n\log n)$
    


##### P4027 NOI2007 Cash 方法二
    令$x[i] = \frac{f[i] \times Rate[i]}{a[i] \times Rate[i]+b[i]},y[i]= \frac{f[i]}{a[i] \times Rate[i]+b[i]}$

    则$f[i] = \max_j (a[i]x[j]+b[i]y[j]) = b[i] \times \max_j(x[j] \frac{a[i]}{b[i]}+y[j])$

    这是一个一次函数的形式，变成了往平面上查询一次函数、查询某个点的最大值的问题

    李超线段树即可
    


##### UVA1106 WF2011 Machine Works
    你的公司获得了一个厂房$N$天的使用权和一笔启动资金，你打算在这$N$天里租借机器进行生产来获得收益。

    可以租借的机器有$M$台。每台机器有四个参数$D,P,R,G$。你可以在第$D$天花费$P$的费用（当然，前提是你有至少$P$元）租借这台机器，从第$D+1$天起，操作机器将为你产生每天$G$的收益。在你不再需要机器时，可以将机器卖掉，一次性获得$R$的收益。

    厂房里只能停留一台机器。

    不能在购买和卖出机器的那天操作机器，但是可以在同一天卖掉一台机器再买入一台。

    在第$N+1$天，你必须卖掉手上的机器。

    求第$N+1$天后能获得的最大资金。
    



##### UVA1106 WF2011 Machine Works
    数据范围

    租借天数$D \le 10^9$
    
    初始资金$C \le 10^9$
    
    机器数$N \le 10^5$
    
    对于每个机器：
    
    租借日$Di \le D$
    
    买入价$P_i$和卖出价$R_i$满足$1 \le R_i<P_i \le 10^9$
    
    每日收益$G_i$满足$1 \le G_i \le 10^9$


##### UVA1106 WF2011 Machine Works
    观察:场地能放机器就一定要放么？

    先将机器按照出售时间$D_i$排序。
    
    将所有时间离散化。
    
    用Fi表示在时刻Di卖掉手上机器后最多剩下多少钱。

    $F_i = \max (F[i-1], F[j]-P[j]+R[j]+G[j] \times (D[i]-D[j]-1))$
    
    条件是$F[j]>P[j]$。
    
    $O(n^2)$



##### UVA1106 WF2011 Machine Works
    $F_i = \max (F[i-1], F[j]-P[j]+R[j]+G[j] \times (D[i]-D[j]-1))$
    
    $F[j]>P[j]$
    
    令$A[j]=F[j]-P[j]+R[j]-G[j] \times D[j]-G[j]$
    
    那么$F[i] = \max(F[i-1],A[j]+G[j] \times D[i])$

    和上一题一样了。
    
    注意到斜率$D[i]$是不变的，因此可以对整个$[F_1,F_n]$进行分治。

    复杂度$O(n\log n)$，和平衡树维护凸壳同阶


##### UVA1106 WF2011 Machine Works

    $F[i] = Max(F[i-1],A[j]+G[j] \times D[i])$

    在平面上有若干直线$y=G[j] \times x+A[j]$

    维护一个直线集，支持以下两类操作
    
    插入一次函数$y=kx+b$
    
    给定询问$D$，求所有函数在$x=d$上的最大值

    维护一系列半平面交

    对偶问题？


##### P4390 BOI2007 Mokia
    有一个$2000000 \times 2000000$的棋盘，每个格子上有一个数字$A[x,y]$，现在要执行两类操作：

    
        - Add $x$ $y$ $a$：$A[x,y] += a$
        - Query $x_0$ $y_0$ $x_1$ $y_1$：询问矩阵$(x_0,y_0)-(x_1,y_1)$内所有格子的数字和。
    

    Add 操作数$ \le 160000$ Query操作数$ \le 10000$


##### P4390 BOI2007 Mokia
    棋盘大小和询问数相差巨大，所以肯定要离散化。
    
    二维线段树维护？
    
    MLE+TLE

    二维树状数组+Hash？
    
    Hash常数过大
    
    空间怎么开？？



##### P4390 BOI2007 Mokia
    和之前的想法类似，定义操作Solve$(L,R)$

    Solve$(L,R)$应当能够处理$[L,R]$之间的所有操作

    Solve$(L,R)$
    
    Solve$(l,mid)$
    
    处理$[l,mid]$中操作对$[mid+1,r]$中操作的影响
    
    Solve$(mid+1,r)$

    如何处理？



##### P4390 BOI2007 Mokia
    前半部分的Add对后半部分的Query造成的影响

    给定带权点集$P=(X_i,Y_i)$，$Q$个询问$(x_0,y_0,x_1,y_1)$，对于每个询问，输出在对应矩形内的点权之和。

    在列上对询问进行差分，将一个询问拆成$2$个
    
    所有点和询问按$Y$排序，线段树维护

    在两维上对询问进行差分，将一个询问拆成4个
    
    所有点和询问按Y排序，树状数组维护
    
    $T(n)=2T(n/2)+O(n\log n)$
    
    $T(n)=O(n\log^2n)$



##### P4169 天使玩偶
    维护二维点集P，支持以下两个操作
    
    
        - 插入点(x,y)
        - 给定询问(x,y)，求点集中离询问点最近的点
        
        距离定义为曼哈顿距离
        
        $Dis(P_1,P_2)=|x_1-x_2|+|y_1-y_2|$
    

    $n,m \le 300000$
    
    $x,y \le 1000000$
    
    k-d树能不能做
    


##### P4169 天使玩偶
    去除$Dis(P_1,P_2)$的绝对值符号
        
    分4种情况讨论：左上，左下，右上，右下
    
    只需要考虑一种情况（答案在询问的左下）
    
    维护点集P，支持以下操作
    
    
        - 将$(x,y)$插入点集
        - 给定询问$(x,y)$，求满足$dis(p_1,p_2)$=$(x-x')+(y-y')=x+y-(x'+y')$最小的点
    

    问题转为求$x'+y'$最大的点
    
    没有操作1的情况
    
    对$x$排序，对$y$维护树状数组



##### P4169 天使玩偶
    定义操作Solve$(l,r)$，处理$[l,r]$之间的询问
    
    Solve$(l,r)$
    
    Solve$(l,mid)$
    
    考虑$[l,mid]$中的点对$[mid+1,r]$中询问的影响
    
    Solve$(mid+1,r)$
    
    给定带权点集$P(x,y)$，权值为$x+y$，给出$Q$个询问$(x,y)$，查找$x' \le x,y' \le y$的最小$x'+y'$。
    
    退化为没有操作$1$的情况
    
    按$X$排序，对$Y$维护树状数组

    $T(n)=2T(n/2)+O(n\log n),T(n)=O(n\log^2n)$



##### UVA1398 Meteor
    有$N$个国家开发小行星的轨道，设立了$M$个观察站。

    观察站从$1$到$M$编号，每个观察站恰好属于一个国家。

    第$i$和第$i+1$个观察站相邻，第$M$个和第$1$个相邻。
    
    科学家预测在接下来的时间里会依次发生$K$场流星雨。
    
    每场流星雨有一个区间$[L_i,R_i]$，表示流星雨波及的区间是从第$L_i$个观察站顺时针数到第$R_i$个。也就是说如果$L_i>R_i$，指的是$L_i,L_i+1,\ldots,m,1,2,…,R_i-1,R_i$。

    每场流星雨有一个数量$A_i$，表示这场流星雨将会为波及的每个观察站提供$A_i$单位的陨石。
    
    每个国家都拥有陨石收集数的目标$W_i$。对于每个国家，输出它在第几场流星雨后可以达到目标。
    
    $N,M,K \le 3 \times 10^5,A_i,W_i \le 10^9$



##### UVA1398 Meteor
    维护观察站信息

    线段树+Lazy-Tag

    差分化+树状数组

    只有$1$个国家的情况
    
    每次流星雨后判断可行性
    
    $O(t \times k \times \log m)$
    
    $T$是国家拥有的空间站数目
    
    二分答案
    
    需要模拟$O(N)$次流星雨后判断可行性
    
    $O(t \times k \times \log k \times \log m)$
    
    为什么？



##### UVA1398 Meteor
    引入分治思想
    
    Solve$(l,r)$用于解决所有答案落在$[L,R]$中的询问
    
    两个参数够不够？
    
    Solve$(l,r,S)$
    
    $S$是一个询问集合，表示需要处理的询问集合
    
    Solve$(l,r,S)$
    
    分割S，S1的答案在$[l,mid]$，S2的答案在$[mid+1,r]$
    
    Solve$(l,mid,S1)$
    
    Solve$(mid+1,r,S2)$


##### UVA1398 Meteor
    分割集合S

    
        - 1)用线段树模拟时刻$Mid$的情况
            
        $Mid$是$O(k)$级别的
        
        $O(k\log m)$
        - 2) 对于$S$中每个国家，查询已收集到的陨石数目

        $O( \sum t \times \log m)$
    
        由于$\sum t(\text{全集})=n$，一层递归的总复杂度$O(n\log m)$
    
       这一步的总复杂度是$O(n\log k\log m)$
    

    (1)的时间复杂度？
    
    $T(x)=2T(x/2)+O(k\log m)$
    
    $T(x)=O(k\log x \log m)$ -> AC?
    
    $T(x)=O(x \times k\log m)$



##### UVA1398 Meteor
    TLE的原因在于$(1)$的单步复杂度和递归长度$x$无关。

    维护全局线段树
    
    $O(\log m)$模拟和撤销一次流星雨
    
    在运行Solve$(l,r)$之前，线段树存储了第$1$次到第$l-1$次流星雨的情况
    
    在运行Solve$(l,r)$之后，线段树存储了第$1$次到第$r$次流星雨的情况
    
    用$+[l,r]$表示模拟$[l,r]$这段流星雨，$-[l,r]$表示撤销

    如何写Solve$(l,r,S)$?



##### UVA1398 Meteor
    Solve$(l,r,S)$
    
    $+[l,mid]$
    
    分割点集$S$
    
    $-[l,mid]$
    
    Solve(l,mid,S1)
    
    Solve(mid+1,r,S2)

    模拟流星雨的时间复杂度
    
    $T(x)=2T(x/2)+O(x\log m)$
    
    $T(x)=O(x\log x\log m)$

    整个算法的复杂度是$O(k\log k\log m+n\log k\log m)$。



##### UVA1398 Meteor
    并行分治

    对于每个国家的询问，一开始的答案区间都是$[1,n]$。
    
    同时对所有国家进行二分查询
    
    主算法Solve执行一次整体二分，调用$\log k$次
    
    每个国家答案区间的中点是$midX$
    
    询问第$X$个国家在$midX$时是否收集到$Wx$的陨石
    
    对$midX$排序，离线处理
    
    顺序模拟所有流星雨，到第midX个时统计答案
    
    如果答案$<Wx$那么区间变成$[mid+1,r]$否则$[l,mid]$
    
    $\log k$次算法后，每个答案区间长度为$1$，直接输出
    
    $T(n)=\log k \times (k \times \log m+n \times \log n)$


##### P3206 HNOI2010 城市建设
    有一个$N$个点$M$条边的无向图，每条边有边权$Wi$
    
    给定$Q$个操作$(Ei,Zi)$，表示将第Ei条边的费用改为$Zi$
    
    每次操作之后，输出当前无向图的最小生成树权值
    
    $N \le 20000,M \le 50000,Q \le 50000,Wi,Zi \le 10^8$
    
    NO SPOILERS PLZ.

    特殊情况：修改后边权不加。



##### P3206 HNOI2010 城市建设

    定义Solve$(l,r)$，用于处理$L$到$R$的操作并得到答案

    Solve$(l,r)$
    
    Solve$(l,mid)$
    
    处理$[l,mid]$的加边对$[mid+1,r]$的询问的影响
    
    Solve$(mid+1,r)$

    一条边对一个询问的影响难以表示。
    


##### P3206 HNOI2010 城市建设
    
    Solve$(l,r)$
    
    Prepare$(l,r)$
    
    Solve$(l,mid)$
    
    Solve$(mid+1,r)$

    对$[l,r]$分治的优势：可能改变的边权数是$O(r-l)$，可能远小于$O(m)$
    
    区间里的MST查询结果有相似性。



##### P2305 NOI2014 购票
    一棵$1$为根的树

    每个点$x$有$5$个参数$f_x,s_x,p_x,q_x,l_x$

    $f_x$表示父亲，$s_x$表示到父亲的边权，$p_x,q_x,l_x$为车票参数

    用$dis_{x,y}$表示树上$x$到$y$的边权和

    从$x$可以坐车到$y$当且仅当$dis_{x,y} \le l_x$，花费为$dis_{x,y} \times p_x+q_x$

    求从每一个点出发，到达$1$号点的最小费用

    $1 \le n \le 2 \times 10^5$


##### P2305 NOI2014 购票
    写出DP式子

    发现是一个斜率优化

    套一个树上CDQ分治


##### 联合省选2023 人员调度
