---
title: Slope-Trick
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';

# Slope-Trick

## 闵可夫斯基和
对于两个点集$A,B$，它们的闵可夫斯基和定义为：

$$
C=\{(x_1+x_2,y_1+y_2|(x_1,y_1) \in A,(x_2,y_2) \in B)\}
$$

性质：$C$的凸包的斜率集合是$A$的凸包的斜率集合和$B$的凸包的斜率集合的并集

这样，我们可以结合Slope-Trick用来优化DP

## 闵可夫斯基和
![G8](/DP/G8.png)

![G9](/DP/G9.png)

##### [Gym103202L Forged in the Barrens](https://codeforces.com/gym/103202/problem/L)
- 给定一个长度为 $n$ 的序列 $\{a_i\}$，你需要将它划分为 $k$ 部分。
- 划分出的每一段定义它的价值为段内的极差，对于所有的 $k = 1, 2, \ldots, n$，求最大的价值和。
- $1 \leq n \leq 2 \times 10^5$，$1 \leq a_i \leq 10^9$。

<CollapsibleComponent title="做法">
平方做法：

对所有区间的极差求最大值，等价于每个区间选两个数相减求最大值。

$f[i][j][0/1/2]$表示前$i$个数划分成$j$段，第$j$段只选了加、只选了减、已经选完了的最大权值。

优化：

$f[i][j]$是一个关于$j$的凸函数

因此可以分治，用$f[l][r][j][0/1/2][0/1/2]$表示区间$[l,r]$分成$j$段，最左边的段、最右边的段状态为$0/1/2$的最大权值

分治的过程中，每次合并左右两侧的段，用闵可夫斯基和的合并，单次合并复杂度线性，总复杂度$O(n\log n)$
</CollapsibleComponent>

##### ABC383G Bar Cover
- 给定一个长度为 $n$ 的序列 $\{a_i\}$，你有若干个长度为 $k$ 的纸条。
- 对于每个 $i = 1, 2, \ldots, \left\lfloor \frac{n}{k} \right\rfloor$，求出恰好放下 $i$ 个不重叠的纸条时（纸条不能超过边界），被纸条覆盖的 $a_i$ 的和的最大值。
- $1 \leq n \leq 2 \times 10^5$，$1 \leq k \leq 5$，$|a_i| \leq 10^9$。

<CollapsibleComponent title="做法">
定义$b[i]=\sum_{j=i}^{i+k-1}a[j]$，则问题变成了从$b$数组中选若干项，每相邻两项间隔$\ge k-1$个数

还是采用分治DP，用$f[l][r][i][t1][t2]$表示区间$[l,r]$选$i$个数，最左边有$t1$个间隔，最右边有$t2$个间隔的最优解

分治的时候需要加剪枝，如果区间长度$\le k$则直接求答案
</CollapsibleComponent>

##### [Gym104128H Factories Once More](https://codeforces.com/gym/104128/problem/H)
给定一棵$n$个点的树，边带权，在树上选$k$个点使它们两两距离和最大。

$1 \le k \le n \le 2 \times 10^5$，边权在$[1,10^9]$之间。

<CollapsibleComponent title="做法">
已经知道了闵可夫斯基和是差分归并，有些题目还会对差分数组整体做些额外的操作，这时候可能就需要些数据结构维护。

常见的维护手段是  dsu on tree 配平衡树，或者可并堆。

首先考虑个暴力的树背包，设 $f_{u,i}$ 表示在 $u$ 子树内选 $i$ 个点的最大答案，转移时考虑统计每一条边会被跨过几次，具体地我们记 $w$ 为 $u$ 到某个儿子 $v$ 的边权，把儿子 $v$ 合并过来时：

$$
f'_{u,x} = \max_{i+j=x} (f_{u,i} + f_{v,j} + wj(k-j))
$$

发现 $wj(k-j) = -wj^2 + wkj$，是个关于 $j$ 的开口向下的二次函数，而我们每次转移形如 $\left( \max , + \right)$ 卷积，所以可以归纳说明我们这个背包是上凸的。所以每次做背包合并时，需要支持给儿子整体加上 $wj(k-j)$，并把差分数组归并。

首先考虑 $wj(k-j)$ 对差分数组的影响，记 $f(x) = -wx^2 + wkx$，那么 $f'(x) = -2wx + wk$，所以差分数组会整体加上一个一次函数。在树上直接归并差分数组复杂度是错的，考虑使用平衡树这种本身就有序的数据结构维护差分数组，给儿子的平衡树打个整体加一次函数的Tag然后直接dsu on tree插入即可，复杂度 $O(n \log^2 n)$。

实现时，为了避免不必要的麻烦可以不插入始终为 $0$ 的 $f_{u,0}$。注意给平衡树加个垃圾回收，不然空间复杂度好像会多个 $\log$。
</CollapsibleComponent>

##### P5073 Ynoi Easy Round 2015

给一个长度为$n$的序列，支持支持全局加法、查询区间最大子段和

$n \le 3 \times 10^5, m \le 6 \times 10^5$

<CollapsibleComponent title="做法">
可以取消修改，把询问变成形如：查询$a[l]+k,a[l+1]+k,\ldots,a[r]+k$的最大子段和

建线段树，在一个区间$[l,r]$中，我们用$pre[i]$表示前$i$项之和，那么区间最大前缀和就是$\max(k \times i + pre[i])$

显然答案只会取到$(i,pre[i])$组成的上凸壳中。同样地，用$suf[i]$表示区间后$i$项之和，然后构造$(i,suf[i])$的凸包。以及用$ans[i]$表示长度为$i$的区间和，构造$(i,ans[i])$的凸包

每次询问先在线段树上定位到log个区间，然后二分到斜率对应的位置，复杂度$mlog^2n$

也可以用把询问排序的技巧，把复杂度优化到$m\log n$
</CollapsibleComponent>

##### 区间k最大子段和
给一个长度为$n$的序列，$m$次询问，每次询问给定$l,r,k$，求从区间$[l,r]$中选k个不相交子段的和的最大值。

$n,m \le 10^5$

<CollapsibleComponent title="做法">
先在线段树上建立凸包，每次询问定位到对应的区间。

怎么限制恰好取$k$段呢？注意到答案关于$k$是凸函数，因此可以采用wqs二分

这样总复杂度$O(q\log V \log^2n)$，一个log是wqs二分，一个log是线段树，一个log是凸包上二分

如果允许离线，可以用整体二分，复杂度少一个log
</CollapsibleComponent>

# Slope-Trick

有时候，一个题很容易列出动态规划方程，转移公式非常简单

但是，会经常遇到状态某一维范围太大的问题

这个时候可以考虑把动态规划的数组，当成一个函数来进行分析

## Slope-Trick

如果一个函数$f(x)$满足以下性质：
    
- $f(x)$连续
- $f(x)$可以分成若干小段，每一小段是一条直线（一次函数）
- $f(x)$是凸/凹函数

则称为slope-trick-able函数

我们用拐点来表示这个函数斜率发生变化的点（即二阶差分不为0的点）

这种函数的性质：

- 两个slope-trick-able函数相加还是slope-trick-able函数
- 两个slope-trick-able函数相加以后的拐点是原来各自拐点的并集

##### 不上升序列（CF713C Sonya and Problem Wihtout a Legend）

给出一个整数序列$A$，每次可以花费$1$的代价给某个数$+1$或者$-1$，求最少需要多少代价使其变成一个递增序列。

$n \le 500000$

<CollapsibleComponent title="做法">    
- 先把题目改为不降，$a'_i = a_i-i$
- $f(x)$表示序列结尾$\le x$时的最优解
- 每次往序列后加一个数，增加的代价是一条折线
- 加入一个数$A$时，$f'(x)=\min(f'(x-1),f(x)+|x-A|)$
- $f(x)$和$|x-A|$都是slope-trick-able函数，只需要把拐点取个并就好了
- 取min就是把折线最右边斜率$\ge 1$的部分删去
- 算法：
- 每次插入两个拐点，然后删除最大的拐点
- 用一个大根堆维护就好了
- 最后还原出$f()$求得答案
</CollapsibleComponent>

##### CF372C Watching Fireworks is Fun
    
- 一个城镇有 $n$ 个区域，从左到右编号为 $1 \sim n$，个区域之间距离 $1$ 个单位距离。
- 有 $m$ 个烟火要放，给定放的地点 $a_i$，时间 $t_i$，如果你当时在区域 $x$，那么你可以获得 $b_i - |a_i - x|$ 的开心值。
- 你每个单位时间可以移动不超过 $d$ 个单位距离，你的初始位置是任意的（初始时刻为 $1$），求你通过移动能获取到的最大的开心值。
- 加强：$1 \leq n \leq 10^9$，$1 \leq m \leq 2 \times 10^5$，$1 \leq a_i, b_i, t_i \leq 10^9$。

<CollapsibleComponent title="做法">
容易发现 $\sum b_i$ 一定会被统计到答案，所以我们只需要最小化 $\sum |a_i - x|$ 即可。

考虑设 $dp_{i,x}$ 表示第 $i$ 个烟花放出来时在位置 $x$ 的答案。

转移形式很简单，记 $\Delta t = t_i - t_{i-1}$:

$$
dp_{i,x} = \min_{y \in [x-\Delta t \times d, x+\Delta t \times d]} dp_{i-1,y} + |a_i - x|
$$

我们仿照上一题的思想，将转移的操作分开来看。首先我们令 $dp'_x = \min_{y \in [x-\Delta t \times d, x+\Delta t \times d]} dp_{i,y}$，也就是找到能走到当前位置的 $dp$ 值的最小值，然后再对 $dp'_x$ 整体加上 $a_i - x$，就得到了 $dp_{i+1,x}$。显然 $dp$ 转移由取 $\min$ 和加一个下凸的 $|a_i - x|$ 两种操作构成，所以 $dp$ 数组本身是下凸的分段一次函数。

然后我们就要考虑这些操作对斜率的影响了。

对于第一个向周围 $\Delta t \times d$ 的邻域内取 $\min$ 的操作，其实就是对拐点进行平移。具体来说对于斜率 $\leq 0$ 的部分因为是单调不升的所以会和右侧的部分取 $\min$，也就是斜率 $\leq 0$ 的拐点向左平移 $\Delta t \times d$ 个单位，而斜率 $> 0$ 的部分因为是单调增的所以会和左边取 $\min$，因此斜率 $> 0$ 的拐点会向右平移 $\Delta t \times d$ 个单位。

然后就是加 $|a_i - x|$ 了，这个就非常简单了，直接把斜率为 $0$ 的拐点和 $a_i$ 的位置关系讨论下就可以了，具体来说假设斜率为 $0$ 的拐点位置为 $l$，斜率为 $1$ 的拐点位置为 $r$（体现在图像上，$[l, r]$ 就是函数取到最小值的那个区间）：

- $l < a_i < r$ 时，$a_i$ 左侧斜率都 $-1$，右侧斜率都 $+1$，相当于 $a_i$ 这个位置加了两个拐点。
- $a_i < l$ 时，相当于 $a_i$ 这个位置斜率减少 $2$ 并成为新的斜率为 $0$ 的拐点，原来的 $l$ 会成为斜率为 $1$ 的拐点。
- $a_i > r$ 时，与上一种情况对称，不再赘述。
    
直接开两个堆分别维护斜率 $\leq 0$ 和 $> 0$ 的拐点，平移可以直接维护两个加法标记，加入 $a_i$ 拐点时取出两个堆顶按照上面的讨论维护即可，值得一提的是@崔化博场上切掉了这一题但是写了棵平衡树来维护。

至于统计答案，可以直接实时维护整个函数的最小值，还是比较方便的。

时间复杂度$O(m\log n)$
</CollapsibleComponent>

##### 玩游戏
白云和白兔喜欢打一款升级游戏。

这款游戏的升级规则是这样的，玩家的初始等级是0。如果单独玩游戏，每玩一个小时可以升一级。同时，游戏还提供了联机组队功能，如果两个人的等级差不超过$C$，则他们可以一起玩，每个小时可以各升两级。

现在，白云和白兔先列举出了自己有空打游戏的时间，白云有$n$个时间段可以打游戏，分别是$[a_1,b_1],[a_2,b_2],\ldots,[a_n,b_n]$，白兔有$m$个时间段可以打游戏，分别是$[c_1,d_1],[c_2,d_2],\ldots,[c_m,d_m]$。在这些时间段内的任何时刻，他们可以打游戏，可以不打游戏，如果时间段有重合,并且等级差不超过$C$，也可以联机打游戏。

现在，白云和白兔想知道，应该如何安排他们打游戏的时间，使得最终白云的等级最高

$n,m \le 2 \times 10^5$

<CollapsibleComponent title="做法">
考虑从左往右DP

用$f[t][i]$表示经过了前$t$个时间段以后，两个人等级差为$i$时，白云最高的等级是多少

转移：

$t$时刻只能白云玩:

$$
f[t][i]=\max(f[t-1][i-1]+1,f[t-1][i])
$$

$t$时刻只能白兔玩:

$$
f[t][i]=\max(f[t-1][i],f[t-1][i+1])
$$

先不考虑同时都能玩的情况，我们考虑函数的形式

前$6$时间只能白云玩，接下来$4$时间只能白兔玩

如果接下来一个单位时间只能白云玩：

![fun2](/DP/fun2.png)

如果接下来一个单位时间只能白兔玩：

![fun1](/DP/fun1.png)

然后考虑白云和白兔都能同时玩的情况

一个重要性质：此时一定会让他们俩都玩（如果选择让白云等一等白兔，不如之前等，没必要现在来等）

所以转移式为：

$$
f[t][i]=\max(f[t-1][i] + 1 + [-C \le i \le C])
$$

样例：$C=2$，先来$6$个只能白云玩的时间段，然后$2$个只能白兔玩的时间段，最后来一个能同时玩的时间段

![fun3](/DP/fun3.png)
    
样例：$C=2$，先来$6$个只能白云玩的时间段，然后$4$个只能白兔玩的时间段，最后来一个能同时玩的时间段

![fun4](/DP/fun4.png)
    
在没有同时玩的时间段时，整个函数是一个差分以后只有$0$和$1$的函数

同时玩的时间段，对应的操作是：把$[-C,C]$区间$+=2$，其它范围$+=1$，会造成这个性质不满足

分析：$[-C,C]$的右侧可能会出现递减，但是递减一定不是最优

$[-C,C]$左侧可能出现斜率$>1$，但是斜率$>1$一定不是最优

</CollapsibleComponent>

##### CF1534G A New Beginning
你在一个二维平面直角坐标系的原点 $(0,0)$ 上，可以向上或向右移动 $1$ 单位长度任意次。

现在给 $n$ 个点，第 $i$ 个点为 $(x_i,y_i)$，求对这 $n$ 个点都打上标记的最少花费。

具体的，在任意时刻，在 $(X,Y)$ 对 $(x,y)$ 打标记，需要花费 $\max(|X-x|,|Y-y|)$ 。

$1 \leq n \leq 8 \times 10^5; 0 \leq x_i,y_i \leq 10^9$

##### ARC072D Dam
    

##### P3642 APIO2016 烟火表演
给一棵以$1$为根的树，每条边有一个边权

每次可以花$1$的代价给某条边权$+1$或者$-1$

问最少花多少总代价，使得根到所有叶子结点的距离相同

点数$\le 3 \times 10^5$

##### CF1210G Mateusz and Escape Room
给出一个 $n$ 个点的环，每个点上都有一堆石子，其中第 $i$ 个点上的石子个数为 $a_i$ 。

每次操作可以选择一堆石子，将其中的一颗石子向左或是向右移到相邻的石子堆中。

要求在进行一系列操作后，位置 $i$ 的石子个数要在 $[L_i,R_i]$ 之间，问最少操作次数?

$n,a_i,L_i,R_i\le 35000$

##### AGC049E Increment Decrement
我们可以对于一个非负整数序列进行如下两种操作任意次。

- 选定一项使其 $+1$ 或者 $-1$，会花费 $1$ 的代价。
- 选定一个区间使区间中的每一项 $+1$ 或者 $-1$，会花费 $C$ 的代价。

定义一个非负整数序列的代价为从全 0 序列变为该序列所需的最小代价。

给定 $n$ 个序列 $B_i$，每个序列长为 $K$，依次将每个 $A_i$ 赋值为 $B_{i,1}, B_{i,2}, \ldots, B_{i,K}$，求所有 $K^n$ 个可能的 $A$ 序列代价和。

##### AGC049E Increment Decrement

##### [37th Petrozavodsk Programming Camp, Summer Dedenne](https://qoj.ac/problem/8087)

##### CF1776E Crossing the Railways
