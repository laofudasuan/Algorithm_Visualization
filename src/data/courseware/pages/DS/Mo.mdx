---
title: 莫队算法
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';
import MoAlgorithmVisualization from '../../../../components/visualizations/MoAlgorithmVisualization.jsx';

# 莫队算法

## 算法介绍

（由于该算法由莫涛发明，并首次在2009年集训队作业提出，故被称为莫队算法）

有一些区间查询问题，直接查询比较难，但如果已经求出了区间$[l,r]$的答案，可以快速推出$[l-1,r],[l+1,r],[l,r-1],[l,r+1]$的答案

莫队算法：将询问按照某种规则排序依次求解，求解完一个询问，将左右端点逐步移动到下一个询问

下面是一个长度为$9$的序列，对$6$个询问的处理过程：

<MoAlgorithmVisualization />

##### P1494 小Z的袜子
给定一个长为$n$的序列，进行$m$次查询，每次查询一个区间$[l,r]$中有多少$(i,j)$
    
满足$l \le i,j \le r$，且$a[i]=a[j]$

<CollapsibleComponent title="做法">
定义$cnt[x]$为$x$的出现次数

假设已经求出了区间$[l,r]$的$cnt$数组

区间移动的时候，如果插入一个$a[i]$

则$ans += 2 \times cnt[a[i]] + 1$

如果删除一个$x$

则$ans -= 2 \times cnt[a[i]] - 1$

</CollapsibleComponent>

## 莫队算法本质
    
在一个平面上有$m$个点，从$u$走到$v$的距离为曼哈顿距离$|l_u-l_v|+|r_u-r_v|$

找一条经过所有点，总距离最小的路径

**曼哈顿距离最小生成树**
    
把每个询问$[l,r]$看做二维平面上的点

于是我们按照建出来的曼哈顿距离最小生成树去DFS

这个做法的复杂度和最优转移一定是同阶的

曼哈顿距离最小生成树和刚刚介绍的排序算法的最坏复杂度是一样的，所以莫队问题可以直接按块排序

## 莫队常数优化

普通莫队卡常数的基本方法

排序要按照奇偶分别排

有些题按照$\sqrt{n}$分块不是最优，可以看情况调整

##### P4396 Ahoi2013 作业
查询区间$[l,r]$中值在$[a,b]$内的不同数个数

$n \le 10^5 , m \le 10^6$

<CollapsibleComponent title="做法">
这是个特殊的三维偏序

由于这个题的特殊性质所以可以用莫队实现

首先可以跑个莫队

维护一个树状数组

复杂度$O( n\sqrt{m}\log n )$

这类莫队有一个通用的优化方法
    
莫队总共修改$O( n\sqrt{m} )$次

查询只有$O( m )$次

树状数组修改和查询复杂度$O( \log n )$

但是如果用值域分块的话

值域分块可以$O( 1 )$修改$O( \sqrt{n} )$查询

于是我们对这个进行根号平衡

修改$O( n\sqrt{m} )$次，每次$O( 1 )$
    
查询$O( m )$次，每次$O( \sqrt{n} )$

总复杂度$O( n\sqrt{m} + m\sqrt{n} ) = O( m\sqrt{n} )$
</CollapsibleComponent>

##### CF617E XOR and Favorite Number

给一个长度为$n$的序列$a$，然后再给一个数字$a$，再给出$m$组询问。每次询问给出一个区间，求这个区间里面有多少个子区间异或和为$k$

$1 \le n,m \le 10^5, 0 \le k,a_i \le 10^6$

<CollapsibleComponent title="做法">
区间异或转化为两个前缀异或

用莫队维护前缀异或的计数器即可
</CollapsibleComponent>

##### P4689 Ynoi2016这是我自己的发明

给一个树，$n$个点，有点权，初始根是$1$

$m$个操作，每次操作：

- 将树根换为 $x$
- 给出两个点 $x，y$，从 $x$ 的子树中选每一个点，$y$ 的子树中选每一个点，如果两个点点权相等，$ans++$，求 $ans$

$n \le 10^5 , m \le 5\times 10^5$

<CollapsibleComponent title="做法">
肯定是按照DFS序转换为区间查询

两个区间不好维护，但是这个信息具有可减性

可以考虑差分
    
按照DFS序转换为区间查询

然后考虑差分

$[l1,r1] - [l2,r2]$的询问

可以差分为：

$F(l1,r1,l2,r2)=F(1,r1,1,r2)-F(1,l1-1,1,r2)-F(1,r1,1,l2-1)+F(1,l1-1,1,l2-1)$
    
这样都变成了前缀的区间

就可以在这个上面跑莫队了

这个题m=5e5，然后这个差分会导致最多差分出9个询问
    
询问最后有5e6个左右，排序的O( mlogm )比较慢
    
可以考虑对莫队的询问进行基数排序
    
总复杂度$O( n\sqrt( m ) + m )$
</CollapsibleComponent>

##### Bzoj3920 Yunna的礼物
序列，查询区间中出现次数$k_1$小的数中值第$k_2$小的数

$n,m \le 5 \times 10^5$

要求线性空间

<CollapsibleComponent title="做法">
我们可以考虑进行高维离散化

比如说，对于一个数x，其在序列中出现了y次

开个```vector<int>v[MAXN]```

在$v[1] , v[2] \ldots v[y]$中都push\_back$(x)$

然后对于每个vector，分别进行离散化

这样就保证了空间线性

高维离散化这个技巧还可以用来在$10^5$的范围下跑二维树状数组

即对于每次树状数组修改的时候，把所有涉及到的节点保存下来，然后对这些节点进行离散化

于是我们离散化之后

就可以边跑莫队边维护一个值域分块来搞了

$O( n\sqrt{m} + m\sqrt{n} ) = O( m\sqrt{n} )$
</CollapsibleComponent>

##### Bzoj4241历史研究

序列，定义$f(l,r,x)$为$x$在区间$[l,r]$中出现次数，查询一个区间中最大的$x \times f(l,r,x)$

<CollapsibleComponent title="做法">    
可能出现的答案只有$n$种

和刚刚那个题类似，一个数$x$出现次数为$y$，则答案有可能为

$x \times 1,x \times 2 \ldots x \times y$

于是将这些可能的值放一起离散化

然后就可以套用值域分块来

$O( n\sqrt{ m } + m\sqrt{n} )$做了
</CollapsibleComponent>

##### P5072 Ynoi2015 盼君勿忘

给你一个序列$a$，每次查询一个区间$[l,r]$。

这个区间一共可以形成$2^{r-l+1}$个子序列，即每个数出现或者不出现，定义一个子序列对答案的贡献为其去重之后的和，即如果一个数$x$在这个子序列里出现了多次，那么只算一次。

查询区间$[l,r]$里面每个子序列的贡献的和。

然而由乃为了让这个题变麻烦，所以每次的膜数不一样。

<CollapsibleComponent title="做法">

考虑单次询问怎么算

对于数$x$，假设出现了$y$次，区间长度是$len$

则$x$对答案的贡献是

$$x(2^{len-y}(2^y-1))=x(2^{len}-2^{len-y})$$

$2^{len-y}$是除了$x$之外的数有这么多个不同的子序列，这些对$x$的贡献没有影响

$2^{y}-1$是所有$x$构成的子序列中有$2^y-1$种至少包含一个$x$，有$1$种不包含$x$
    
如果每次模数一样的话，直接边跑莫队维护就可以了。。。

然而每次模数不一样
    
注意到贡献分为两部分$x2^{len}$与$-x2^{len-y}$

其中第一部分非常好维护
    
第二部分的贡献，可以把出现次数相同的数一起维护贡献
    
注意到一个区间中只有$O( \sqrt{n} )$种不同的出现次数

因为$1+2+...+\sqrt{n} = O(n)$

这是一个自然根号

所以我们可以用一个均摊的莫队来维护区间可能的出现次数，从而维护区间中所有出现次数

然后为了$O(1)$实现快速幂，我们可以每次$O( \sqrt{n} )$算出

$2^1,2^2…2^{\sqrt{n}} \% p$

以及$2^{\sqrt{n}},2^{2\sqrt{n}},\ldots,2^{\sqrt{n} \times \sqrt{n}} \% p$
    
总复杂度$O( n\sqrt{m} + m\sqrt{n}) = O( m\sqrt{n} )$
</CollapsibleComponent>

<CollapsibleComponent title="做法2">
我们可以直接在快速幂上跑类似finger search的东西

如果维护了sorted的次幂，我们已知$2^x$，需要求$2^y$，保证$x \le y$，则可以$O( \log(y-x) )$处理，而不用$O( \log y )$的时间

这样可以证明复杂度是$O( \sqrt{n} )$，大家可以利用高数知识自己证一下
</CollapsibleComponent>

##### P3245 HNOI2016大数
给一个数字串以及一个质数$p$

多次查询这个数字串的一个子串里有多少个子串是$p$的倍数

<CollapsibleComponent title="做法">
记$suf[i]$为$i \to n$构成的后缀串

如果对于$l,r$有$suf[l] \% p == suf[r+1] \% p$

则$s[l \ldots r] \times 10 ^ { n - r - 1 }$为$p$的倍数

对$p=2,5$时特判

$p \neq 2,5$时$s[l \ldots r] \times 10 ^ { n - r - 1 }$为$p$的倍数即意味着$s[l \ldots r]$为$p$的倍数
    
离散化一下，然后就变成小Z的袜子了
</CollapsibleComponent>

##### 区间众数

给一个长为$n$的序列，有$m$次询问，每次询问一个区间中出现次数最多的一个元素的出现次数

<CollapsibleComponent title="做法">
莫队转移时答案最多$+1$或者$-1$，所以直接维护即可

总时间复杂度$O(n\sqrt{m})$
</CollapsibleComponent>

##### P3604 美好的每一天
给一个小写字母的字符串

每次查询区间有多少子区间可以重排成为一个回文串

$n,m \le 6 \times 10^4$

<CollapsibleComponent title="做法">
什么条件等价于重排成为一个回文串？

- 所有数都出现偶数次
- 只有一个数出现奇数次

有什么转换的形式方便维护一个数出现的奇偶性？

奇偶性可以想到异或

我们令$a \to 1,b \to 2,c \to 4,\ldots,z \to 2^{25}$

如果维护一个前缀xor和$b$，$b[i]=b[i-1]$ xor $(2^{a[i]}-1)$

则一个区间$[i,j]$的xor和等于$[1,i-1]$ xor $[1,j]$，也就是等于$b[i-1]$ xor $b[j]$

发现出现次数都是偶数等价于$b[i-1]$ xor $b[j]=0$

出现次数都是奇数等价于$b[i-1]$ xor $b[j]=2^k,k=0 \ldots 25$

于是问题就变成了

区间有多少二元组$(i,j)$满足$b[i-1]$ xor $b[j]==0,1,2,4,\ldots,2^{25}$

每次for $26$个元素即可

设字符集为$c$

$O( n\sqrt{n}c )$
</CollapsibleComponent>

### 图分块

##### HDU4858

给一张$n$个点$m$条边的图，每个点有权值，有$q$次操作，每次操作为修改或询问

修改：单点加法

询问：查询一个点的所有邻居权值之和

##### P10559 The Last Cumulonimbus Cloud
你有一个 $n (1 \leq n \leq 3 \times 10^5)$ 个点，$m (1 \leq m \leq 3 \times 10^6)$ 条边的无向图。

保证这个图的任意一个非空子图都至少有一个点的度数小于等于 $10$。

每个点初始有一个权值 $a_i$。

现在要执行 $q (1 \leq q \leq 2 \times 10^6)$ 次操作：

- 1 $x$ $v$ 给第 $x$ 个点的权值加 $v$。
- 2 $x$ 查询第 $x$ 个点所有相邻的点的权值之和（不包括自己）。

<CollapsibleComponent title="做法">
每次找到一个度数$\le 10$的点将其删除，这样得到一个删除顺序

然后将原图定向，删除早的向删除晚的连边，这样每个点出度不超过$10$

维护每个点入度点权之和，修改时枚举所有出边
</CollapsibleComponent>

# 回滚莫队

- P3247 HNOI2016 最小公倍
- JOISC 2014 Day1历史研究

##### CF1137F Matches Are Not a Child's Play

给定一棵无根树，共 $n$ 个节点，每个节点有优先值 $p_i$，初始 $p_i = i$。定义 “删除序列” 为每次删除优先值最小的叶子节点得到的节点编号序列。

需要支持以下操作共 $q$ 次：
- 修改：将一个节点的 $p_i$ 更新为 $\max_{1 \leq i \leq n} p_i + 1$。
- 查询：查询一个节点在删除序列上的位置。
- 比较：比较两个节点在删除序列上的位置谁更靠前。

$n, q \leq 2 \times 10^5$，时限 $4\text{s}$。

<CollapsibleComponent title="做法"> 
先把无根树转化为有根树，可以令 $p$ 最大的点为根——它必定最后被删除。

之后考虑一次查询过程，查询 $x$ 时找出子树内 $p$ 最大的节点 $y$，显然是 $y$ 把它卡住了删不掉。之后标记所有 $p_i \geq p_y$ 的节点和其祖先，必定是把树删到只剩下这些节点，然后从 $y$ 一路删到 $x$。

这已经可以处理比较操作了，但为了解决查询操作需要找出所有 $p_i \geq p_y$ 的节点，求包含它们的最小连通块（注意到根节点的 $p$ 一定 $\geq p_y$）的节点数量。

这里可以用 LCT 直接做，但也有另一种处理方案。

令 $1$ 为根，把所有特殊节点按照 dfs 序排序。之后求出它们的深度之和减去相邻两个位置的 LCA 深度之和，得到的就是包含这些节点和 $1$ 的最小连通块大小；为了去掉根节点的影响，求出所有节点的 LCA（也就是 dfs 序最小和最大的节点的 LCA）即可。

但是我们还需要快速找出这些节点。注意到全程一个 $p$ 至多出现一次，可以令 $a_i$ 代表唯一一个存在任意时刻满足 $p_x = i$ 的 $x$。

之后找出 $p_i \geq p_y$ 的节点就相当于取出 $a$ 上面 $[p_y, p_r]$ 这一段（$r$ 为当前 $p$ 最大的节点）。

现在尝试用莫队维护一个特殊点组成的集合，需要在维护的集合中插入或者删除一个点。求 LCA 可以做到单次 $O(1)$，但是在集合中插入删除点需要找到它两侧的第一个点，这需要 $O(\log n)$ 的复杂度，无法接受。

利用 WC2022 秃子酋长一题的技巧，使用只删除的回滚莫队，这样维护一个按照 dfs 序排好序的双向链表，就能直接查询前驱后继。

综上，假设 $n,q$ 同阶，总复杂度 $O(n\sqrt{n})$，需要注意算法常数。
</CollapsibleComponent>

# 树上莫队

# 半平面莫队

- P8529 [Ynoi2003] 赫露艾斯塔
