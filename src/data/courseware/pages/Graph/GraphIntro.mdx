---
title: 图的基本概念
---

import GraphAlgorithmVisualization from '../../../../components/visualizations/GraphAlgorithmVisualization';

# 图的基本概念

## 1. 图的定义

图是一种重要的非线性数据结构，由节点（顶点）集合和边集合组成。在计算机科学中，图被广泛应用于表示各种复杂的数据关系，如社交网络、交通网络、互联网拓扑等。

**图的数学定义**：图 G 由两个集合组成，记为 G = (V, E)，其中：
- V 是顶点（节点）的有限非空集合
- E 是边的有限集合，每条边连接两个顶点

根据边是否有方向，图可以分为：
- **无向图**：边没有方向，连接顶点u和v的边可以表示为(u,v)或(v,u)
- **有向图**：边有方向，从顶点u指向v的边表示为(u,v)

下面是一个无向图示例：

<GraphAlgorithmVisualization graphName="ExampleGraph-500-300" enableAnimation={false}/>

## 2. 图的存储

在计算机中，图有多种存储表示方法，不同的存储方式适用于不同的应用场景和操作需求。下面介绍两种最常用的图存储方式。

### 2.1 邻接矩阵存储

邻接矩阵是一种使用二维数组表示图的方法。对于有n个顶点的图，邻接矩阵是一个n×n的矩阵A，其中：
- 对于无向图：A[i][j] = 1（或边的权值），如果顶点i和j之间有边；否则A[i][j] = 0
- 对于有向图：A[i][j] = 1（或边的权值），如果存在从顶点i到j的边；否则A[i][j] = 0

邻接矩阵的优点是可以在O(1)时间内判断两个顶点之间是否有边相连，但空间复杂度为O(n²)，对于稀疏图可能会浪费较多空间。

下面是一个图及其对应的邻接矩阵表示：

<div className="my-8">
  <GraphAlgorithmVisualization 
    graphName="dfs-graph" 
    enableAnimation={false}
    showAdjacencyMatrix={true}
  />
</div>

上图右侧显示的是该图的邻接矩阵，其中：
- 值为1的单元格（蓝色背景）表示两个顶点之间有边相连
- 值为0的单元格（白色背景）表示两个顶点之间无边相连
- 邻接矩阵是对称的，因为这是一个无向图

### 2.2 邻接表存储

邻接表是一种使用链表或数组存储图的方法。对于有n个顶点的图，邻接表由n个链表（或数组）组成，其中第i个链表存储与顶点i相邻的所有顶点。

邻接表的优点是空间复杂度为O(V+E)，对于稀疏图非常节省空间，而且查找一个顶点的所有邻居也很高效。但判断两个顶点之间是否有边相连需要O(degree(v))的时间。

下面是一个图及其对应的邻接表表示：

<div className="grid grid-cols-1 md:grid-cols-2 gap-8 my-8">
  <div className="rounded-lg p-4 bg-white shadow-sm" style={{ height: '350px' }}>
    <h4 className="text-center mb-4 text-gray-700">图结构可视化</h4>
    <GraphAlgorithmVisualization width={400} height={300} graphName="dfs-graph" />
  </div>
  
  <div className="rounded-lg p-4 bg-white shadow-sm">
    <h4 className="text-center mb-4 text-gray-700">邻接表表示</h4>
    <div className="space-y-3">
      <div className="flex items-center p-2 bg-gray-50 rounded">
        <span className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center mr-3 font-bold">0</span>
        <span className="text-gray-700">→</span>
        <span className="ml-3 flex gap-3">
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">1</span>
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">3</span>
        </span>
      </div>
      
      <div className="flex items-center p-2 bg-gray-50 rounded">
        <span className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center mr-3 font-bold">1</span>
        <span className="text-gray-700">→</span>
        <span className="ml-3 flex gap-3">
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">0</span>
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">2</span>
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">4</span>
        </span>
      </div>
      
      <div className="flex items-center p-2 bg-gray-50 rounded">
        <span className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center mr-3 font-bold">2</span>
        <span className="text-gray-700">→</span>
        <span className="ml-3 flex gap-3">
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">1</span>
        </span>
      </div>
      
      <div className="flex items-center p-2 bg-gray-50 rounded">
        <span className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center mr-3 font-bold">3</span>
        <span className="text-gray-700">→</span>
        <span className="ml-3 flex gap-3">
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">0</span>
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">4</span>
        </span>
      </div>
      
      <div className="flex items-center p-2 bg-gray-50 rounded">
        <span className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center mr-3 font-bold">4</span>
        <span className="text-gray-700">→</span>
        <span className="ml-3 flex gap-3">
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">1</span>
          <span className="w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-xs">3</span>
        </span>
      </div>
    </div>
    <p className="text-sm text-gray-600 mt-4">注：蓝色圆圈表示顶点，绿色圆圈表示其相邻的顶点</p>
  </div>
</div>

## 3. 图的遍历

图的遍历是指从图中的某个顶点出发，按照一定的顺序访问图中的所有顶点，且每个顶点只被访问一次。常见的图遍历算法有深度优先搜索（DFS）和广度优先搜索（BFS）。

### 3.1 深度优先搜索（DFS）

深度优先搜索是一种递归的遍历算法，它尽可能深地沿着一条路径前进，直到无法继续前进时，才回溯到前一个节点，然后尝试另一条路径。

**DFS算法步骤**：
1. 将起始节点标记为已访问，并将其压入栈中
2. 当栈不为空时：
   - 查看栈顶节点
   - 如果该节点有未访问的相邻节点，选择其中一个，将其标记为已访问，并压入栈中
   - 如果该节点的所有相邻节点都已访问，则将其从栈中弹出（回溯）
3. 重复步骤2，直到栈为空

### 3.2 广度优先搜索（BFS）

广度优先搜索是一种层次遍历算法，它从起始节点出发，先访问其所有相邻节点，然后再访问这些相邻节点的相邻节点，以此类推，按照距离起始节点由近及远的顺序遍历图中的所有节点。

**BFS算法步骤**：
1. 将起始节点加入队列，并标记为已访问
2. 当队列不为空时：
   - 从队列头部取出一个节点
   - 访问该节点
   - 将该节点的所有未访问过的相邻节点加入队列尾部，并标记为已访问
3. 重复步骤2，直到队列为空

下面是图遍历算法的可视化演示：

<GraphAlgorithmVisualization graphName="dfs-graph" enableAnimation={true}/>

**DFS与BFS的比较**：
- **数据结构**：DFS使用栈（递归调用栈或显式栈），BFS使用队列
- **遍历顺序**：DFS是深度优先，BFS是广度优先
- **应用场景**：
  - DFS适用于连通性检测、拓扑排序、寻找路径等
  - BFS适用于寻找最短路径、层序遍历等
- **空间复杂度**：在最坏情况下，DFS的空间复杂度为O(V)，BFS的空间复杂度也为O(V)
- **时间复杂度**：两者的时间复杂度均为O(V+E)，其中V是顶点数，E是边数