---
title: 反射容斥
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';
import FunctionPlot from '../../../../components/visualizations/FunctionPlot';

# 反射容斥

### 卡特兰数

见数列-卡特兰数

##### P14973 『GTOI - 2D』木棍

##### Gym104053J

长度为 $n$，值域在 $\{-1, 1\}$ 的序列，任意前缀和 $\in [0, m]$ 的方案数。

$m \le n \le 10^7$。

<CollapsibleComponent title="解法"> 
一个对复杂度的小应用。

注意到在预处理组合数之后复杂度是 $O\left(\frac{n}{m}\right)$ 的，所以直接枚举序列的和是多少，再跑反射容斥即可。

时间复杂度 $O(n)$。
</CollapsibleComponent>

##### CF1967E1 Again Counting Arrays (Easy Version)

有一个无穷大的棋盘，你最开始在 $(0, b_i)$ 处。
第 $1 \sim n$ 列各有一个障碍 $(i, a_i)$。
你要从起点走 $n$ 步，每次朝右上 $(x+1, y+1)$ 或者右下 $(x+1, y-1)$ 走一步，且不能走到障碍。
给定 $m$，问 $a_i \in [1, m]$ 的 $m^n$ 个 $a$ 中，有多少可以做到全程保持 $y \ge 0$。

答案对 $998244353$ 取模。

- Easy: $n, m, b_0 \le 2 \times 10^5$，5 秒。
- Hard: $n, m, b_0 \le 2 \times 10^6$，3 秒。

<CollapsibleComponent title="解法"> 

- **Hint 1:** 对于一个确定的 $a$，如何计算答案？

  每次障碍最多限制我们一个前进的方向。
  因为 $y$ 越大越容易走到底，所以如果能往上走，我们一定会往上走。
  如果走到边界，即 $y = m$，我们显然可以一直往上走，此时必定合法。

- **Hint 2:** 可以写一个 $O(nm)$ 时间复杂度的暴力吗？

  可以将 DP 式子写成格路计数的形式。
  相当于，碰到 $y < 0$ 会死，碰到 $y = m$ 会活，向上走一步权值是 $m-1$，向下走一步权值是 $1$。

  反射容斥的形式是：我们碰到两边都会死。

所以不妨直接枚举我们在什么时候第一次碰到顶上，或者全程没碰到顶上。

此时碰到两边都会死，就可以上反射容斥了，时间复杂度

$$
O\left((n + m)\frac{n}{m}\right) = O\left(n + \frac{n^2}{m}\right)
$$

反射容斥的部分走了几次上几次下是固定的，后面的选择也是完全随意的。

剩下的部分是一个搞笑的脑梗部分：直接 DP 是 $O(nm)$的，总复杂度 $O(n \sqrt{n})$。

</CollapsibleComponent>

##### CF1967E2

先不说枚举终点会让复杂度还是 $O\left(\frac{n^2}{m}\right)$ 的，单是正确性就不对了……

例子：走到 $< 0$ 关于 $y = -1$ 对称之后可能会出现没有碰线的情况，即这样的格路与不合法路径不构成双射。
好在这类情况还是可以处理的，将没有碰线的再容斥掉即可。

关于复杂度，我们会发现，此时所有容斥中，要求的都是

$$
\binom{n}{i}(m-1)^i
$$

我们可以将这些值做前后缀和，就可以一次对所有位置回答询问。

时间复杂度精细实现就是 $O(n)$ 的，但是注意反射到奇奇怪怪的位置会涉及较多的边界细节。