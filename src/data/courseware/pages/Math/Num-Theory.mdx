---
title: 数论
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';

# 数论

### 证明

给定正整数 $n>2$。设不超过 $n/2$ 的与 $n$ 互素的最大整数为 $f(n)$。求证：

$$
f(n) =
\begin{cases}
\dfrac{n-1}{2}, & n \equiv 1,3 \pmod{4} \\[6pt]
\dfrac{n}{2}-1, & n \equiv 0 \pmod{4} \\[6pt]
\dfrac{n}{2}-2, & n \equiv 2 \pmod{4}
\end{cases}
$$

### 证明

**Erdős–Ginzburg–Ziv 定理**：在任意的 $2n-1$ 个数中，总有 $n$ 个数它们的和是 $n$ 的倍数。

本题中你不需要证明这个定理。你只需要证明如下结论：

如果上述定理对于正整数 $n$ 和 $m$ 都成立，那么它对于 $nm$ 也成立。

### 证明

当 $\gcd(a_1,\dots,a_n)=1$，充分大的正数可表示成 $a_1 s_1 + \dots + a_n s_n$（$s_i$ 都非负）

**提示**：1. 归纳法。2. 只需要证明有一个模 $a_1$ 余 $1$ 的数能表示出来即可。

### 四. 《具体数学（第二版）》第 4 章-17 题：

假设 $f[n] = 2^{(2^n)} + 1$。
证明当 $m < n$ 时，$f[m]$ 与 $f[n]$ 互素。

### 五. 《具体数学（第二版）》第 4 章-38 题：
假定 $\gcd(a,b)=1$，$a>b$，$0 \le m < n$，$a,b,m,n$ 都是整数。
求证：$\gcd(a^m - b^m, a^n - b^n) = a^{\gcd(m,n)} - b^{\gcd(m,n)}$

**提示**：欧几里得算法。

### 六. 已知 A 和 B 是两个非空字符串。

$R(A) = AAA\ldots$（无限长的字符串，不断循环 A）
$R(B) = BBB\ldots$（无限长的字符串，不断循环 B）

求证：$R(A) = R(B) \iff AB = BA$

#### 阅读作业

阅读《具体数学（第 2 版）》4.5 节“互素（relative primality）”。特别是，了解这一节中介绍的 **Stern-Brocot Tree**。这个树巧妙地组织了所有既约分数。下面这题与既约分数有关联。

### 七 本题是一道 noi+/ctsc-级别的思维题

有三个变量。一开始它们分别存储了正整数 $a,b,c$（$a,b,c$ 是输入），比如 $(4,6,11)$。变量可存储很大的数值。

每一步你可以将两个变量相加，相加的结果存入第三个变量。比如 $(4,6,11)$ 可以变为 $(4,6,10)$、$(4,15,11)$、$(17,6,11)$。

请问，是否存在一种操作方案，使得三个变量中至少一个的值变为 $N$。你只需要输出 **"Yes" / "No"**。

**数据范围**：$1 \le a,b,c \le 30000$，$1 \le N \le 10^{100}$