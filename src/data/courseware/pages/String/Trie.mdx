---
title: 字典树
---

import { CollapsibleComponent } from '../../../courseware/markdownConfig';

# 字典树

## 基础功能

维护一个字符串集合并支持快速查询。

##### IOI2008 type printer

你需要利用一台可移动的打印机打印出 N 个单词。这种可移动式打印机是一种老式打印机，它需要你将一些小的金属块（每个包含一个字母）放到打印机上以组成单词。然后将这些小金属块压在一张纸上以打印出这个词。这种打印机允许你进行下列操作：
- 在打印机当前词的末端（尾部）添加一个字母；
- 在打印机当前词的尾部删去一个字母（将打印机当前词的最后一个字母删去）。仅当打印机当前至少有一个字母时才允许进行该操作；
- 将打印机上的当前词打印出来。
初始时打印机为空，或者说它不含任何带字母的金属块。打印结束时，允许有部分字母留在打印机内。同时也允许你按照任意的次序打印单词。
由于每一个操作都需要一定时间，所以需要你尽可能减少所需操作的总数目（将操作的总数最小化）。


##### THUSC 2016 补退选
在补退选阶段，学生即可以选课，也可以退课。对于老师来说，在补退选阶段可能发生以下两种事件：
- 一个姓名为S的学生选了他的课
- 一个姓名为S的学生退了他的课
同时，老师对于有哪些学生选了他的课非常关心，所以他会不定时的查询已选课学生名单。

每次查询的格式如下:最早在哪个事件之后，姓名以$S$为前缀的学生数量超过了$v$。

注意：

- 学生的姓名可能相同，如果有p个姓名相同的学生都选了X老师的课，则他们的姓名将出现在X老师的名单上p次。
- 只有已经选了课的学生才会退课，如果姓名为S的学生退课，则在他退课之前X老师的名单上一定有姓名S。
- 选课,退课和查询都被定义为"事件","事件"的编号从1开始

<CollapsibleComponent title="做法">
字典树套vector?
</CollapsibleComponent>

##### 字符串查询
给定$n$个字符串和$q$个询问

每次询问在这$n$个字符串中，有多少个字符串同时满足

字符串$a$是它的前缀

字符串$b$是它的后缀

$n,q \le 50000$，字符串长度$\le 100$

##### P9694 [GDCPC 2023] New but Nostalgic Problem

给定 $n$ 个字符串 $w_1, w_2, \cdots, w_n$，请选出恰好 $k$ 个字符串，最小化字符串 $v$ 的字典序，并输出这个最优的字符串 $v$。其中 $v$ 满足以下条件：$v$ 是被选出的字符串中，某两个编号不同的字符串的最长公共前缀。而且，$v$ 是所有满足条件的字符串中，字典序最大的字符串。

更正式地，令 $\mathbb{S}$ 表示一个大小为 $k$ 的集合，集合中的元素均为从 $1$ 到 $n$ 的整数（含两端），且没有重复的元素。令 $\text{lcp}(w_i, w_j)$ 表示字符串 $w_i$ 和 $w_j$ 的最长公共前缀，您需要找到一个集合 $\mathbb{S}$ 以最小化下述字符串 $v$ 的字典序，并输出这个最优的字符串 $v$。

$$
v = \max\limits_{i \in \mathbb{S}, j \in \mathbb{S}, i \ne j} \text{lcp}(w_i, w_j)
$$

上式中的 $\max$ 通过字典序比较两个字符串。

请回忆：
- 称字符串 $p$ 是字符串 $s$ 的前缀，若可以在 $p$ 的末尾添加若干个字符（包括零个字符）将它变成 $s$。特别地，空字符串是任意字符串的前缀。
- 字符串 $s$ 和 $t$ 的最长公共前缀是一个最长的字符串 $p$，满足 $p$ 既是 $s$ 的前缀，又是 $t$ 的前缀。例如，``abcde`` 与``abcef`` 的最长公共前缀为 ``abc``，而 ``abcde`` 与 ``bcdef`` 的最长公共前缀为空字符串。
- 称字符串 $s$ 的字典序小于字符串 $t$（$s \ne t$），若
  - $s$ 是 $t$ 的前缀，或
  - $s_{|p| + 1} < t_{|p| + 1}$，其中 $p$ 为 $s$ 和 $t$ 的最长公共前缀，$|p|$ 为 $p$ 的长度，$s_i$ 表示字符串 $s$ 的第 $i$ 个字符，$t_i$ 表示字符串 $t$ 的第 $i$ 个字符。
- 特别地，空字符串是字典序最小的字符串。

##### CTSC2011 字符串重排
有$n$个由小写字母组成的两两不同的非空字符串集合$S_{1 \ldots n}$，对于一个$1$到$n$的排列$p$，定义$p$的价值$w(p)$如下

$$
W(P)=\sum_{i=2}^n {\left(lcp(S_{p_{i-1}},S_{p_i})\right)}^2
$$

我们需要最大化$W(P)$

此外，还有$q$个附加任务，对于第$i$个任务，给定两个$1$到$n$之间两两不同的整数$x_i,y_i$，对于排列P，在W(P)保持最优的前提下，同时满足$pos(S_{x_i}+1)==pos(S_{y_i})$，则排列P还将获得$2^i$的奖励。所有任务的奖励之和称之为总任务奖励。

求在保证$W(P)$最大的前提下，可以使总任务奖励最大的排列。

<CollapsibleComponent title="做法"> 
结论：构建字典树之后，所有W最大的方案为对树任意的DFS遍历路径所对应的字符串序列

由于任务的奖励是按照幂次递增的，故按照任务的倒序能满足就立刻满足并修正当前序列，不能满足就跳过

对于任务:$S_a$要在$S_b$之前，令x为$LCA(S_a,S_b)$

得到的限制：
- $S_a$为u子树中最后一个访问的串
- $S_b$为v子树中第一个访问的串
- 对于$x$，DFS时先访问$u$子树,再访问$v$子树

对这几种情况分类讨论，用线段树维护限制。

由于非常复杂这里就不写了。具体做法可以看CTSC官方PPT。
</CollapsibleComponent>

##### 例题

给出n个字符串$S_{1..n}$,还有m个限制$a_{1..n},b_{1..n}$。

现在可以把这n个串任意排列，求满足$\forall i,S_{a_i}$是$S_{b_i}$的前缀的方案数。

$n\le50,1\le|s_i|\le50,m\le8$

<CollapsibleComponent title="做法">
- 前缀有关的问题，很容易联想到Trie。
- 我们先建立一棵Trie。注意到$m\le8$，很显然应该是状压DP。
- 用$f[i][s]$表示i的子树中已经填好了位置集合s的方案数。
- 如何去掉不满足条件的情况？如果不满足，说明$\exists i,a_i \in s,b_i \notin s$
- 我们可以先预处理出合法的集合。这样就可以直接转移了。
- 合法集合的个数是$3^m$级别，用$(3^m)^2$预处理合法转移后，转移的复杂度是$5^m*n$
</CollapsibleComponent>

## 字典树的空间优化
如果一个结点只有一条出边，那么我们可以把它和它的孩子合并，然后记录这条边上压缩的字符。

这样处理后结点数最多为$2n-1$。

## 二进制字典树

字典树在求二进制整数的问题上也有很大作用。

把一个整数看成一个二进制下的01串。我们用Trie来存储这个01串。

### 例题

给定$n$个数，求两两异或最大值。

位运算的题目一般都是可以按位贪心的，那么我们可以在Trie上一步一步地确定答案的每一位。

##### CF1055F Tree and XOR
给一棵$n$个点的树，边有边权

求$n^2$个点对中，第$k$小的路径异或和

$n \le 10^6, w < 2^{62}$，时间限制$4S$

<CollapsibleComponent title="做法">

一条路径的异或，等于两个端点各自到根的异或再异或起来。问题变成了：给$n$个数，求第$k$小的异或pair

考虑按位确定。每次确定$0$开头的有没有$k$个，有$k$个就确定$0$，否则$k-=0$的对数，然后确定$1$

在字典树上一步一步走
</CollapsibleComponent>

##### P14363 CSP-S 2025 谐音替换 / replace


##### P6623 省选联考 2020 A 卷 树

给一棵树，每个点有点权。

对于每棵子树，求子树内，每一个点的点权+到根的距离的异或和。

$n,v_{1 \ldots n} \le 5 \times 10^5$

<CollapsibleComponent title="做法">
从下往上，用字典树维护每棵子树

需要支持的操作：整体+1

字典树如何实现整体+1？从低位往高位建字典树，每次交换左右子树，递归新的0子树
</CollapsibleComponent>

##### P14509 树上求值 tree




