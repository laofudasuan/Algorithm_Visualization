画图软件：

帮我调整一下GraphCanvas、animateGraph、drawingTools这三个文件的结构：

GraphCanvas.jsx是一个React的组建，可以被插入在其他页面之中

GraphCanva的参数：
- 宽度、高度
- 图的数量

包含变量：
- 当前显示的图索引

包含函数：
- 切换当前图索引
- 修改当前图，调用AnimateGraph的主图层修改函数
- 增加指示，调用AnimateGraph的临时指示器修改函数

初始化：
- 根据图的数量，创建对应数量的元素，所有图初始的宽度和高度与容器一致
- 初始化当前图索引为0
- 在右上角列出所有图索引序号，点击后可以切换当前显示的图

animateGraph.jsx 是一个「图可视化 + 手绘批注」二合一的 React 容器组件
1. 提供三层离屏 Canvas（主图层 / 批注层 / 指示器层），支持节点拖拽、画笔、橡皮擦、导出 PNG/SVG 等操作
2. 自适应容器大小、高清屏 oversample、画布展开/收缩等体验细节

下面展示三层分别的用途
main         | refMain        | 是       | 节点、边、布局动画
annotation   | refAnnotation  | 是       | 手绘笔迹、橡皮擦
indicator    | refIndicator   | 否       | 临时指示（鼠标指针、hover）

animateGraph.jsx类包含以下参数：
| 参数名 | 类型 | 默认值 | 作用 |
| --- | --- | --- | --- |
| width | number | 1000 | 画布宽度 |
| height | number | 600 | 画布高度 |
| nodes | array | [] | 节点数据数组，包含每个点的编号、位置、大小、颜色、位置是否固定 |
| edges | array | [] | 边数据数组，包含每条边的编号、起点、终点、颜色、宽度 |
| nodesStyle | object | {} | 节点默认样式 |
| edgesStyle | object | {} | 边默认样式 |
| mode | string | 'all' | 允许修改的模式，可选值为 'all'（允许所有修改操作）、'static'（只允许修改颜色、宽度等静态属性）、'none'（不可修改） |

主图层包含以下函数：
- 修改画布高度、宽度
- 修改点、边数组
- 修改点的样式、边的样式
- 布局函数，可以选择不同的布局算法，如力导向布局、树状布局等，选择树状布局需要选择根节点
- 力导向更新函数，当图修改完之后调用，计算出更新完之后的再传给drawingTools.js进行绘制

批注层包含以下函数：
- 批注修改函数

指示器层包含以下函数：
- 鼠标交互函数
- 画面中增加临时注释函数

drawingTools.js负责具体图的绘制、动画（动画用animejs库实现）
所有图案的配置均在animateGraph.jsx被计算完成，被放入drawingTools.js中调用

包含以下函数：
- 绘制节点。给每个点建一个单独的anime元素，首次出现时做淡入动画，中间更新时做移动动画，被删除时做淡出动画
- 绘制边。给每条边建一个单独的anime元素，首次出现时做淡入动画，中间更新时做移动动画，被删除时做淡出动画
- 绘制批注。给每个批注建一个单独的anime元素，首次出现时做淡入动画，中间更新时做移动动画，被删除时做淡出动画


实现内容：

1. 1.
   基础3D场景搭建
   
   - 创建了完整的Three.js三要素（相机-场景-渲染器）
   - 添加了环境光和方向光以增强视觉效果
   - 实现了OrbitControls控制器支持视图交互
2. 2.
   3D力导向布局优化
   
   - 实现了自定义力导向算法，确保关联节点靠近、非关联节点远离
   - 为节点设置随机初始位置，然后应用引力算法调整布局
   - 使用SphereGeometry渲染节点，LineBasicMaterial渲染关系
3. 3.
   空间穿梭体验
   
   - 实现了点击节点触发相机平滑移动的空间穿梭功能
   - 使用requestAnimationFrame和缓动函数实现相机移动的平滑过渡
   - 相机自动聚焦到选中节点并更新控制器目标
4. 4.
   交互功能增强
   
   - 节点悬停效果：悬停时节点放大并高亮显示
   - 节点信息面板：显示选中节点的标签和类型
   - 重置视图功能：一键返回全局视图
   - 响应式设计：支持窗口大小变化自动调整
5. 5.
   性能优化
   
   - 实现了资源清理，避免内存泄漏
   - 使用材质复用减少内存占用
   - 添加了加载状态管理和错误处理
文件修改：

- 重写了 `KnowledgeGraph.jsx` 组件，使用纯Three.js实现3D知识图谱
现在您可以通过以下方式与3D知识图谱交互：

- 点击任意节点进行空间穿梭
- 拖动鼠标旋转视图，滚轮缩放
- 悬停在节点上查看标签
- 点击重置按钮返回全局视图

成功为您实现了交互式课件模块，包含以下功能和文件：

创建的文件：

1. 1.
   课件数据目录： src/data/courseware/
2. 2.
   示例课件文件： courseware-001.json 和 courseware-002.json
3. 3.
   课件列表页面： src/pages/CoursewareList.jsx
4. 4.
   课件详情页面： src/pages/CoursewareDetail.jsx
实现的功能：

- 课件列表展示，包含标题、描述、作者和创建时间信息
- 点击课件卡片进入详情页的交互功能
- 支持渲染多种内容格式：
  - 文本内容显示
  - 使用KaTeX渲染数学公式
  - 图片占位符显示
  - 页面跳转链接，可连接到其他可视化页面
- 平滑的动画效果，提升用户体验
技术实现：

- 使用React Router实现路由导航
- 集成KaTeX库用于数学公式渲染
- 使用framer-motion实现流畅的动画效果
- 采用JSON格式存储课件数据，方便后续扩展
更新的文件：

- src/App.jsx - 添加了课件相关的路由配置和导航菜单项
现在您可以通过导航菜单中的"交互式课件"进入课件列表页面，点击任意课件即可查看详细内容。后续您只需在 src/data/courseware/ 目录下添加新的JSON格式课件文件，系统就会自动加载并显示这些课件。