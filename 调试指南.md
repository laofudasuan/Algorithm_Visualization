# 项目单步调试指南

## 方法一：使用浏览器开发工具调试

这是最简单直接的调试方式，适用于大多数前端开发场景：

### 步骤：

1. **启动开发服务器**
   ```bash
   npm run dev
   ```

2. **打开浏览器开发者工具**
   - Chrome/Firefox: 按 F12 或右键点击页面 → 检查
   - Safari: 先在偏好设置 → 高级 → 勾选「在菜单栏中显示开发菜单」，然后点击顶部菜单栏的「开发」→ 「显示Web检查器」

3. **单步调试JavaScript代码**
   - 切换到「Sources」(Chrome) 或「调试器」(Firefox) 标签页
   - 在左侧文件树中找到你要调试的文件（通常在`localhost:5173/src/`目录下）
   - 点击代码行号添加断点
   - 刷新页面或触发相关交互，代码执行到断点时会暂停
   - 使用调试控制按钮进行单步执行：
     - ▶️ 继续执行
     - ⬇️ 单步执行（进入函数）
     - ➡️ 单步跳过
     - ⬆️ 单步跳出

4. **查看变量和调用栈**
   - 暂停时，右侧面板会显示当前作用域的变量值
   - 可以在「监视」面板添加特定变量进行跟踪
   - 「调用栈」面板显示代码执行的路径

## 方法二：使用VS Code进行调试

VS Code提供了更集成的调试体验，特别是对于动画相关代码：

### 配置步骤：

1. **安装必要扩展**
   - VS Code中安装「Debugger for Chrome」或「Debugger for Firefox」扩展

2. **创建调试配置**
   - 点击VS Code左侧边栏的调试图标（🔍）
   - 点击顶部的「创建 launch.json 文件」
   - 选择「Chrome」或「Firefox」
   - VS Code会自动生成基本配置

3. **修改launch.json文件**
   - 打开`.vscode/launch.json`文件
   - 修改配置如下（以Chrome为例）：
   ```json
   {
     "version": "0.2.0",
     "configurations": [
       {
         "type": "chrome",
         "request": "launch",
         "name": "调试Vite应用",
         "url": "http://localhost:5173",
         "webRoot": "${workspaceFolder}",
         "sourceMaps": true,
         "userDataDir": false,
         "skipFiles": ["<node_internals>/**"]
       }
     ]
   }
   ```

4. **启动调试**
   - 先运行`npm run dev`启动开发服务器
   - 回到VS Code，点击调试界面的绿色▶️按钮开始调试
   - VS Code会打开一个新的Chrome窗口，自动连接到调试器

5. **使用VS Code调试功能**
   - 在VS Code中直接点击代码行号添加断点
   - 使用VS Code顶部的调试控制按钮
   - 在调试面板中查看变量、监视表达式和调用栈

## 方法三：使用React DevTools

对于React组件状态和属性的调试特别有用：

1. **安装React DevTools扩展**
   - Chrome/Firefox: 从各自的扩展商店安装
   - 或使用独立应用：`npm install -g react-devtools`

2. **启动开发服务器**
   ```bash
   npm run dev
   ```

3. **使用React DevTools**
   - 打开浏览器开发者工具，切换到「Components」或「Profiler」标签
   - 可以查看组件树、检查组件属性和状态
   - 使用「Profiler」工具分析组件渲染性能

## 针对动画组件的特定调试技巧

对于我们修改的动画相关代码，有一些特定的调试技巧：

### 1. 动画帧调试

在`animateGraph.jsx`或`drawingTools.js`中添加调试代码：
```javascript
// 在动画函数中添加
console.log('Animation frame:', { nodeId, progress, currentProps });
```

### 2. 使用动画控制变量

在组件中添加临时的动画控制变量，方便调试：
```javascript
const [debugAnimation, setDebugAnimation] = useState(false);

// 在动画函数中
if (debugAnimation) {
  // 减慢动画速度
  animationDuration *= 5;
  // 添加更详细的日志
  console.log('Debug animation:', { target, duration, easing });
}
```

### 3. 在浏览器控制台中调试

可以在浏览器控制台中直接访问组件实例（如果使用了ref）：
```javascript
// 假设你的组件使用了ref并挂载到window对象
window.graphComponent.controller.animateNode('1', { x: 100, y: 100 });
```

## 常见调试问题及解决方法

1. **断点不生效**
   - 确保源代码映射(source maps)已启用（Vite默认启用）
   - 尝试清除浏览器缓存并刷新页面
   - 检查断点位置是否正确

2. **动画太快难以观察**
   - 使用浏览器的FPS限制功能（Performance标签页）
   - 在代码中临时减慢动画速度
   - 使用requestAnimationFrame的回调来逐步观察变化

3. **状态变化不反映**
   - 使用React DevTools检查组件状态
   - 确保使用setState正确更新状态
   - 检查依赖项数组是否正确（在useEffect中）

4. **Canvas绘制问题**
   - 在Canvas绘制前后添加console.log记录绘制参数
   - 使用ctx.save()和ctx.restore()隔离绘制操作
   - 考虑使用辅助网格或边界框显示元素位置

希望这些调试方法对你有所帮助！根据具体需求，你可以选择最适合的调试方式。